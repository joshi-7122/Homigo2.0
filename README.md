# Homigo2.0
# Onsitego UI Replica 
A simple Python web app built with Streamlit that replicates the UI of the Onsitego homepage.

## How to Run Locally
1. Clone this repository.
2. Install the required dependencies:
   `pip install -r requirements.txt`
3. Run the app:
   `streamlit run app.py`
